{
  "scripts": {
    "dev:react": "concurrently \"npm run dev --prefix backend\" \"npm run dev:react --prefix frontend\"",
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\""
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
