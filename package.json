{
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix frontend\" \"npm run start:dev --prefix electron\"",
    "build": "npm run build --prefix backend && npm run build --prefix frontend && npm run build --prefix electron",
    "dist": "npm run build && npm run dist --prefix electron"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
